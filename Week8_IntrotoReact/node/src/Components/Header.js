
const Header = (props) => {
    return (
        <div>
            <h1>{props.section}</h1>
        </div>
    )
}

// Header.defaultProps = {
//     subject:"unknown",
//     adj:"don't know"
// }

export default Header

